require 'spec_helper'

describe Kantox::Matcher do
  it 'has a version number' do
    expect(Kantox::Matcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
